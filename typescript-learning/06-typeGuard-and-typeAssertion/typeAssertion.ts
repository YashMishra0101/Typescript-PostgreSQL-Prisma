/*
>Type assertion
Type assertion in TypeScript is a way of telling the TypeScript compiler to treat a value as a specific type, without actually changing the value at runtime. (means we are telling to typeScript to what value it is)

-- Type Assertion can be done in two ways:

1. Using the "as" syntax
const input = element as HTMLInputElement;

2. Using the angle bracket "<>" syntax
const input = <HTMLInputElement>element;
*/

// const emailInput = document.querySelector("#email") as HTMLInputElement;

// const showEmail = document.querySelector("#showEmail") as HTMLButtonElement;

// const result = document.querySelector("#result") as HTMLParagraphElement;

// console.log(emailInput.value);

// showEmail.addEventListener("click", () => {
//   result.textContent = `Your email is ${emailInput.value}`;
// });
